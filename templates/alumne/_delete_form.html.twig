<form method="post" action="{{ path('alumne_delete', {'id': alumne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alumne.id) }}">
    <button class="btn">Delete</button>
</form>

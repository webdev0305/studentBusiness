<form method="post" action="{{ path('practica_delete', {'id': practica.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ practica.id) }}">
    <button class="btn">Delete</button>
</form>
